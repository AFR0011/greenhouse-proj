{
  "name": "api",
  "version": "1.0.0",
  "description": "API for Greenhouse Control System",
  "main": "sync.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 start npm --name sync -- run start-sync && pm2 start npm --name notifications -- run start-notifications && pm2 logs",
    "start-sync": "node sync.js",
    "start-notifications": "node notifications.js"
  },
  "author": "Ali Farrokhnejad",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.2",
    "depd": "^2.0.0",
    "express": "^4.19.2",
    "firebase-admin": "^12.0.0"
  }
}
